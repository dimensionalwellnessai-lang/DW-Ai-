# Database
DATABASE_URL=postgresql://user:password@localhost:5432/fts

# Session
SESSION_SECRET=your-session-secret-here

# AI Integration (OpenAI-compatible API)
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1
AI_INTEGRATIONS_OPENAI_API_KEY=your-openai-api-key

# External Content Discovery APIs (Optional)
# YouTube Data API v3 - https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=

# NewsAPI - https://newsapi.org/
NEWS_API_KEY=

# API-Ninjas Exercise Database - https://api-ninjas.com/
EXERCISE_API_KEY=

# Resend (Email) - Required for password reset functionality
# Option 1: Set RESEND_API_KEY directly (works on any platform)
# Get your API key at https://resend.com
RESEND_API_KEY=
# Must be a valid "from" value supported by Resend, e.g. DW.ai <no-reply@your-verified-domain.com>.
# Replace "your-verified-domain.com" with a domain you have verified in your Resend dashboard.
RESEND_FROM_EMAIL=no-reply@your-verified-domain.com

# The public URL of your app, used in password reset email links and OAuth redirect URIs
APP_URL=https://dimensionalwellnessai.com

# ─── OAuth (Social Sign-In) ────────────────────────────────────────────────────
# Base URL used to build OAuth callback/redirect URIs.
# Defaults to APP_URL if not set.
# Change this when migrating from Replit to a custom domain.
OAUTH_REDIRECT_BASE_URL=https://dimensionalwellnessai.com

# Google OAuth — https://console.cloud.google.com/apis/credentials
# Create an "OAuth 2.0 Client ID" (Web application) and add the callback URI:
#   https://dimensionalwellnessai.com/api/auth/google/callback
# Both vars must be present to enable the "Continue with Google" button.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple Sign In — https://developer.apple.com/account/resources/identifiers
# 1. Register a Services ID (use it as APPLE_CLIENT_ID) and enable "Sign In with Apple".
# 2. Add the return URL:  https://dimensionalwellnessai.com/api/auth/apple/callback
# 3. Create a Sign In with Apple key and download the .p8 file.
# 4. Paste the entire .p8 contents as APPLE_PRIVATE_KEY (use literal \n for newlines if needed).
# All four vars must be present to enable the "Continue with Apple" button.
APPLE_CLIENT_ID=com.example.yourapp
APPLE_TEAM_ID=ABCDE12345
APPLE_KEY_ID=XXXXXXXXXX
# Multi-line PEM — paste the full contents of your .p8 file, replacing real newlines with \n:
APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----
